# Starknet interaction (0.29.0 is the first release supporting Python 3.13)
starknet-py>=0.29.0

# Async HTTP (price feeds)
aiohttp>=3.9.0

# Configuration management
python-dotenv>=1.0.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Structured logging
structlog>=24.1.0

# Faucet HTTP server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Metrics (optional — disable with ENABLE_METRICS=false)
prometheus-client>=0.20.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0
aioresponses>=0.7.6
